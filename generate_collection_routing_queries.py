# Simplified `generate_queries`

# This just generates queries that require the LLM to select one of the collections

# Load Schemas JSON

# For schema in schemas:
#   For collection in schema:
#     CreateObjects()